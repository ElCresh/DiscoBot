# Uploaded media
media/

# SoundFonts (binary, user-provided)
soundfonts/

# Cover art cache (downloaded album art, may be copyrighted)
coverart_cache/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
.cache

# Player state
state.json

# Runtime config (toggle live, generato al primo avvio)
runtime_config.json

# Manager auth (password hash + secret, NON committare)
manager_auth.json

# Manager active sessions (cookie ids, ip, user-agent — privacy)
manager_sessions.json

# Environment
.env

# Spotify librespot session credentials
.spotify_credentials.json
credentials.json
